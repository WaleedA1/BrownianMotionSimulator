Brownian Motion Simulator built using random walk models with the NumPy and MatPlotLib. Please install the following libraries in the terminal before running with the following:
`pip install numpy 
pip install matplotlib`
